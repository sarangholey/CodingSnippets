package SeleniumSessions.Sikuli;

import java.util.regex.Pattern;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;

import com.gargoylesoftware.htmlunit.javascript.host.Screen;

public class YoutubevideoMute {

	static WebDriver driver;
	
	/*
	 * public static void main(String[] args) {
	 * System.setProperty("webdriver.chrome.driver", "chromedriver.exe"); driver =
	 * new ChromeDriver();
	 * 
	 * 
	 * driver.get("https://www.youtube.com/watch?v=SkRC6RFyLbE");
	 * 
	 * Screen s = new Screen(); Pattern pauseImg = new Pattern("YT_Mute");
	 * s.wait(pauseImg, 2000); s.click();
	 * 
	 * 
	 * }
	 */

}
